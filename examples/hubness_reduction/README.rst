============================================
Example: Hubness reduction
============================================

This example shows how to perform hubness reduction in kNN classification
in nested cross-validation to select the best model and assess its performance.
